---
/**
 * The crepey-neck / neck-lines guide (/neck) — problem-template variant of the
 * collagen-guide layout: evidence strength as the primary visual signal, one
 * interactive block (problem matcher).
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import MediaPlaceholder from '../components/MediaPlaceholder.astro';
import ExpandableDrawer from '../components/ExpandableDrawer.astro';
import EvidenceRow from '../components/EvidenceRow.astro';
import { DEFAULT_LOCALE } from '../i18n/ui';
import {
  keyTakeaways,
  groups,
  focusLabels,
  allSections,
  readingMinutes as guideReadingMinutes,
  type Evidence,
} from '../data/neck';

const locale = DEFAULT_LOCALE;
const title = 'Crepey neck & neck lines — every fix graded | DeAging Europe';
const description =
  'Crepey texture, horizontal neck lines, platysmal bands and laxity — creams, injectables, energy devices and surgery graded by neck-specific evidence, with prices.';

const readingMinutes = guideReadingMinutes();

const evidenceRank: Record<Evidence, number> = { strong: 0, moderate: 1, emerging: 2, limited: 3 };

const evidencePillClass = (e?: Evidence) =>
  e === 'strong'
    ? 'bg-sage/15 text-sage'
    : e === 'moderate'
    ? 'bg-gold/15 text-gold'
    : e === 'emerging'
    ? 'bg-rose/15 text-rose'
    : 'bg-ink/10 text-ink/60';

const orderedSections = (g: (typeof groups)[number]) =>
  g.sections
    .slice()
    .sort((a, b) => evidenceRank[a.evidence ?? 'limited'] - evidenceRank[b.evidence ?? 'limited']);

const faqAnswer: Record<string, string> = {
  'faq-cream': 'For hours',
  'faq-tretinoin': 'Yes, gently',
  'faq-botox': 'Bands only',
  'faq-lines': 'Soft HA filler',
  'faq-ultherapy': 'Mild laxity only',
  'faq-kybella': 'Yes, Rx-only',
  'faq-neck-lift': 'Pinch test fails',
};

const topInfoIds = new Set(['basics', 'context']);
const infoGroupIds = new Set([...topInfoIds, 'safety', 'faq']);
const topInfoGroups = groups.filter((g) => topInfoIds.has(g.id));
const mainGroups = groups.filter((g) => !topInfoIds.has(g.id));
const evidenceGroups = mainGroups.filter((g) => !infoGroupIds.has(g.id));
const tailInfoGroups = mainGroups.filter((g) => infoGroupIds.has(g.id));
const lastChartGroupId = evidenceGroups[evidenceGroups.length - 1].id;
const partNo = (g: (typeof groups)[number]) => mainGroups.findIndex((x) => x.id === g.id) + 1;

// Interactive matcher — problem → recommended rows.
const matcher: { concern: string; paths: { id: string; title: string; why: string }[] }[] = [
  {
    concern: 'Crepey, crinkly texture',
    paths: [
      { id: 'home-retinoid', title: 'Retinoid + daily SPF', why: 'the base — everything else is undone without it' },
      { id: 'dev-nafl', title: 'Non-ablative fractional laser', why: 'the resurfacing class the neck tolerates' },
    ],
  },
  {
    concern: 'Red-brown mottling on the neck and chest',
    paths: [{ id: 'dev-ipl', title: 'IPL / BBL', why: 'the best-evidenced energy treatment on the neck — >75% clearance in 3 sessions' }],
  },
  {
    concern: 'Horizontal "necklace" lines',
    paths: [{ id: 'inj-ha-lines', title: 'HA filler / skin boosters', why: '~1.4 grades of improvement; half re-treat within a year' }],
  },
  {
    concern: 'Vertical cords when I grimace (platysmal bands)',
    paths: [
      { id: 'inj-toxin', title: 'Botulinum toxin', why: 'two phase-3 trials — the one neck injectable with pivotal data' },
      { id: 'surg-neck-lift', title: 'Neck lift', why: 'when bands are thick and present at rest' },
    ],
  },
  {
    concern: 'Double chin (fat, skin still springs back)',
    paths: [
      { id: 'inj-deoxycholic', title: 'Deoxycholic acid', why: 'permanent, two ~500-patient trials, a swollen fortnight' },
      { id: 'dev-cryolipolysis', title: 'Cryolipolysis', why: '~20% fat-layer reduction per cycle, less swelling' },
    ],
  },
  {
    concern: 'Mild looseness, no fat, skin still elastic',
    paths: [
      { id: 'dev-ultherapy', title: 'Ultherapy', why: 'subtle lift in ~2 of 3, 12–18 months' },
      { id: 'dev-rf-microneedling', title: 'RF microneedling', why: 'conservative depths only on a thin neck' },
    ],
  },
  {
    concern: 'A fold I can pinch that stays ("turkey neck")',
    paths: [
      { id: 'candidacy-prices', title: 'Device or surgery?', why: 'the pinch test decides — read this before spending on devices' },
      { id: 'surg-neck-lift', title: 'Neck lift', why: 'the only fix for true skin excess: 8–12 years' },
    ],
  },
];

// Durability-ladder comparison cards.
const ladderRows = [
  {
    id: 'inj-toxin',
    step: 'Toxin for bands',
    duration: '3–4 months',
    evidence: 'strong' as Evidence,
    blurb: 'Relaxes the cords and the downward pull — €250–450 a session.',
  },
  {
    id: 'inj-ha-lines',
    step: 'HA for horizontal lines',
    duration: '~12 months',
    evidence: 'moderate' as Evidence,
    blurb: 'Fills the crease; about half of patients repeat within a year.',
  },
  {
    id: 'dev-ultherapy',
    step: 'Ultherapy for laxity',
    duration: '12–18 months',
    evidence: 'moderate' as Evidence,
    blurb: 'Subtle, temporary tightening for elastic skin — not a fold.',
  },
  {
    id: 'surg-neck-lift',
    step: 'Neck lift',
    duration: '8–12 years',
    evidence: 'strong' as Evidence,
    blurb: 'Resets the angle; ~2% hematoma, ~1% nerve injury in pooled data.',
  },
];
---

<BaseLayout title={title} description={description} locale={locale} path="neck" type="article">
  <article class="bg-bone">
    <!-- ════════════ HERO ════════════ -->
    <header class="relative border-b border-ink/10 overflow-hidden">
      <div class="max-w-6xl mx-auto px-6 py-14 md:py-20 grid lg:grid-cols-[1fr_auto] gap-10 items-center">
        <div>
          <p class="text-xs uppercase tracking-[0.3em] text-sage mb-4">Problem · Neck</p>
          <h1 class="font-display text-5xl md:text-7xl leading-[1.02] max-w-3xl">
            The neck, honestly<span class="text-rose">.</span>
          </h1>
          <p class="mt-6 max-w-2xl text-lg md:text-xl text-ink/75 leading-relaxed">
            Crepey texture, necklace lines, platysmal bands and laxity &mdash; four different
            problems, every fix graded from cream to surgery.
          </p>
          <p class="mt-3 text-xs text-ink/50">
            Updated 3 Sep 2026 · ~{readingMinutes} min full read · {allSections().length} sections
          </p>
        </div>
        <MediaPlaceholder kind="image" ratio="4/5" label="Hero — profile portrait" tone="rose" class="w-full lg:w-72" />
      </div>
      <div class="absolute -right-32 -top-32 w-96 h-96 bg-rose/10 rounded-full blur-3xl pointer-events-none"></div>
    </header>


    <!-- ════════════ TOP-FOLD SUMMARY + INFO DRAWERS ════════════ -->
    <section id="takeaways" class="scroll-mt-32 max-w-6xl mx-auto px-6 py-12 space-y-10">
      <div>
        <p class="text-xs uppercase tracking-[0.3em] text-sage">The case in five lines</p>
        <h2 class="font-display text-3xl mt-2">If you read nothing else</h2>
        <ol class="mt-5 space-y-3 text-[15px] leading-relaxed text-ink/85 max-w-3xl">
          {keyTakeaways.map((k, i) => (
            <li class="flex gap-3">
              <span class="font-display text-rose w-6 shrink-0">{i + 1}.</span>
              <span>{k}</span>
            </li>
          ))}
        </ol>
      </div>

      <div>
        <MediaPlaceholder kind="video" ratio="16/9" label="Explainer · 2 min" tone="sage" />
      </div>

      {topInfoGroups.map((g) => (
        <section id={g.id} class="scroll-mt-32 pt-2">
          <h2 class="font-display text-3xl md:text-4xl">{g.title}</h2>
          <div class="mt-6 grid gap-2">
            {g.sections.map((s) => (
              <ExpandableDrawer id={s.id} title={s.title} bodyHtml={s.bodyHtml} />
            ))}
          </div>
        </section>
      ))}
    </section>

    <!-- ════════════ MAIN CONTENT (the graded chart) ════════════ -->
    <section class="bg-white border-y border-ink/5">
      <div class="max-w-6xl mx-auto px-6 py-12">

        <div class="mb-10 pt-4">
          <p class="text-xs uppercase tracking-[0.3em] text-sage">The full breakdown</p>
          <h2 class="font-display text-4xl md:text-5xl lg:text-6xl mt-2 leading-[1.05]">The neck — what the evidence says</h2>
        </div>

        <div id="guide-body" class="min-w-0">
          {evidenceGroups.map((g) => (
          <>
          <section id={g.id} class="group-section scroll-mt-32 mb-16" data-group={g.id}>
            <p class="text-xs uppercase tracking-[0.3em] text-sage">Part {String(partNo(g)).padStart(2, '0')}</p>
            <h2 class="font-display text-3xl md:text-4xl mt-2">{g.title}</h2>
            {g.intro && <p class="mt-3 text-ink/70 max-w-2xl">{g.intro}</p>}

            {g.id === 'devices' && (
              <div class="mt-6 grid md:grid-cols-2 gap-4">
                <MediaPlaceholder kind="video" ratio="16/9" label="Ultherapy on the neck, step by step" tone="rose" />
                <MediaPlaceholder kind="image" ratio="16/9" label="The four neck problems, mapped" tone="bone" />
              </div>
            )}

            <div class="card-grid mt-8 divide-y divide-ink/[0.07] border-y border-ink/[0.07]">
                {orderedSections(g).map((s) => (
                  <EvidenceRow
                    s={s}
                    variant="bar"
                    tag={s.focus && s.focus !== 'general' ? focusLabels[s.focus] : undefined}
                    mediaTone={g.id === 'devices' || g.id === 'surgery' ? 'rose' : undefined}
                  />
                ))}
            </div>
          </section>

          {g.id === lastChartGroupId && (
            <div id="picks" class="mb-16 scroll-mt-32">
              <p class="text-xs uppercase tracking-[0.3em] text-sage">Editor's choice</p>
              <h2 class="font-display text-3xl md:text-4xl mt-2">Our picks</h2>
              <p class="mt-3 text-ink/70 max-w-2xl">One per problem — where the neck-specific evidence is actually good.</p>

              <div class="mt-8 grid sm:grid-cols-2 lg:grid-cols-4 gap-5">
                {[
                  {
                    id: 'home-retinoid',
                    kind: 'The base',
                    title: 'Retinoid + SPF to the chest',
                    blurb: 'The only prevention with trial data — and the tolerance trick is to start low and buffer.',
                    tone: 'sage' as const,
                  },
                  {
                    id: 'inj-toxin',
                    kind: 'For bands',
                    title: 'Botulinum toxin',
                    blurb: 'The one neck injectable with pivotal trials; 3–4 months per session.',
                    tone: 'rose' as const,
                  },
                  {
                    id: 'dev-ipl',
                    kind: 'For mottling',
                    title: 'IPL for poikiloderma',
                    blurb: 'Twenty years of series: >75% clearance of red-brown mottling in ~3 sessions.',
                    tone: 'gold' as const,
                  },
                  {
                    id: 'surg-neck-lift',
                    kind: 'For a real fold',
                    title: 'A neck-lift consultation',
                    blurb: 'Before the devices, not after — the only fix for skin you can pinch.',
                    tone: 'bone' as const,
                  },
                ].map((pick) => (
                  <a
                    href={`#${pick.id}`}
                    class="pick-card group flex flex-col bg-white rounded-2xl border border-ink/10 overflow-hidden hover:border-rose hover:shadow-md transition"
                  >
                    <MediaPlaceholder kind="image" ratio="4/3" label={pick.title} tone={pick.tone} />
                    <div class="p-5 flex flex-col flex-1">
                      <p class="text-[10px] uppercase tracking-widest font-semibold text-sage">{pick.kind}</p>
                      <h3 class="font-display text-lg leading-tight mt-1 group-hover:text-rose transition">{pick.title}</h3>
                      <p class="mt-2 text-sm text-ink/65 leading-snug flex-1">{pick.blurb}</p>
                      <span class="mt-4 inline-flex items-center gap-1 text-xs uppercase tracking-widest font-semibold text-rose group-hover:gap-2 transition-all">
                        View details
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <line x1="5" y1="12" x2="19" y2="12"></line>
                          <polyline points="12 5 19 12 12 19"></polyline>
                        </svg>
                      </span>
                    </div>
                  </a>
                ))}
              </div>
            </div>
          )}
          </>
        ))}
        </div>
      </div>
    </section>

    <!-- ════════════ SAFETY / FAQ TAIL + MATCHER / LADDER / REFERENCES ════════════ -->
    <div class="max-w-6xl mx-auto px-6 py-12">

        {tailInfoGroups.map((g) => (
          <section id={g.id} class="group-section scroll-mt-32 mb-16" data-group={g.id}>
            <p class="text-xs uppercase tracking-[0.3em] text-sage">Part {String(partNo(g)).padStart(2, '0')}</p>
            <h2 class="font-display text-3xl md:text-4xl mt-2">{g.title}</h2>
            {g.intro && <p class="mt-3 text-ink/70 max-w-2xl">{g.intro}</p>}
            <div class="mt-8 grid gap-2">
              {g.sections.map((s) => (
                <ExpandableDrawer id={s.id} title={s.title} short={faqAnswer[s.id] ?? s.tldr} bodyHtml={s.bodyHtml} />
              ))}
            </div>
          </section>
        ))}

        <!-- ════════════ PROBLEM MATCHER ════════════ -->
        <section id="matcher" class="scroll-mt-32 mb-16 bg-gradient-to-br from-rose/10 via-bone to-sage/5 rounded-2xl p-6 md:p-10 border border-ink/5">
          <p class="text-xs uppercase tracking-[0.3em] text-sage">Interactive</p>
          <h2 class="font-display text-3xl md:text-4xl mt-2">Match a treatment to your neck</h2>
          <p class="mt-2 text-ink/70 max-w-2xl">Open the problem you actually have — each link jumps to the graded section.</p>

          <div class="mt-8 grid gap-2">
            {matcher.map((m) => (
              <details class="matcher-item group rounded-2xl border border-ink/10 bg-white px-4 py-3 transition hover:border-rose/40 open:border-rose/40">
                <summary class="cursor-pointer list-none flex items-center justify-between gap-4">
                  <h3 class="font-display text-xl leading-tight group-hover:text-rose group-open:text-rose transition">{m.concern}</h3>
                  <span class="shrink-0 w-8 h-8 rounded-full flex items-center justify-center text-rose bg-rose/10 group-open:bg-rose group-open:text-white transition" aria-hidden="true">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="transition-transform group-open:rotate-180">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </span>
                </summary>
                <div class="mt-3 pt-3 border-t border-ink/10 space-y-2">
                  {m.paths.map((p) => (
                    <a href={`#${p.id}`} class="block p-3 rounded-xl bg-bone/50 border border-ink/5 hover:border-rose/40 transition">
                      <span class="font-medium text-ink/90">{p.title}</span>
                      <span class="text-ink/60"> — {p.why}</span>
                    </a>
                  ))}
                </div>
              </details>
            ))}
          </div>
        </section>

        <!-- ════════════ DURABILITY LADDER ════════════ -->
        <section id="compare" class="scroll-mt-32 mb-16 bg-white rounded-2xl border border-ink/5 p-6 md:p-8 overflow-hidden">
          <p class="text-xs uppercase tracking-[0.3em] text-sage">The trade</p>
          <h2 class="font-display text-3xl md:text-4xl mt-2">How long each fix lasts</h2>
          <p class="mt-2 text-ink/65 max-w-2xl">Nothing except surgery and fat destruction is permanent — budget for maintenance of whatever works.</p>

          <div class="mt-6 grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
            {ladderRows.map((r) => (
              <a href={`#${r.id}`} class="block p-5 bg-bone/40 rounded-xl border border-ink/5 hover:border-rose/40 hover:shadow-md transition group">
                <div class="flex items-center justify-between gap-2 mb-2">
                  <span class={`text-[10px] uppercase tracking-widest font-semibold px-2 py-0.5 rounded-full ${evidencePillClass(r.evidence)}`}>{r.evidence}</span>
                </div>
                <h3 class="font-display text-lg leading-tight group-hover:text-rose">{r.step}</h3>
                <p class="mt-1 text-sm text-ink/65 leading-snug">{r.blurb}</p>
                <p class="mt-3 text-xs font-medium text-ink/85">Lasts: {r.duration}</p>
              </a>
            ))}
          </div>
        </section>

        <!-- ════════════ REFERENCES ════════════ -->
        <section id="references" class="scroll-mt-32 bg-white rounded-2xl p-8 border border-ink/5">
          <h2 class="font-display text-3xl">References & further reading</h2>
          <p class="mt-2 text-ink/70 text-sm">
            All claims cite peer-reviewed studies, pivotal-trial reports, or regulatory documents,
            linked inline within each section. Primary sources: PubMed/PMC, Dermatologic Surgery,
            Aesthetic Surgery Journal, PRS Global Open, Lasers in Surgery and Medicine, FDA and EMA
            documents.
          </p>
        </section>

        <p class="mt-10 text-xs text-ink/50 italic max-w-3xl">
          Educational content, not medical advice. Neck treatments carry neck-specific risks —
          assess them with a qualified clinician, disclose thyroid disease, and see a surgeon before
          spending on devices if the pinch test fails.
        </p>
    </div>

  </article>

  <style>
    details > summary::-webkit-details-marker { display: none; }
    details > summary { list-style: none; }

    details > summary:focus-visible {
      outline: 2px solid var(--color-rose);
      outline-offset: -2px;
      border-radius: 1rem;
    }

    .card[open] > summary { background: rgba(184,130,109,0.04); }

    section[id], details[id] {
      scroll-margin-top: calc(var(--header-h, 60px) + var(--bar-h, 56px) + 12px);
    }

    @media print {
      aside, .pointer-events-none { display: none !important; }
      details { break-inside: avoid; }
      .card { break-inside: avoid; border: 1px solid #ddd !important; box-shadow: none !important; }
    }
  </style>

  <script is:inline>
    (function () {
      const guideBody = document.getElementById('guide-body');
      if (!guideBody) return;

      function measureChrome() {
        const header = document.querySelector('body > header');
        document.documentElement.style.setProperty('--header-h', (header?.getBoundingClientRect().height || 60) + 'px');
        document.documentElement.style.setProperty('--bar-h', '0px');
      }
      measureChrome();
      window.addEventListener('resize', measureChrome);

      function openHashTarget() {
        if (!location.hash) return;
        const el = document.getElementById(location.hash.slice(1));
        if (el?.tagName === 'DETAILS') el.setAttribute('open', '');
      }
      window.addEventListener('hashchange', openHashTarget);
      openHashTarget();
    })();
  </script>
</BaseLayout>
