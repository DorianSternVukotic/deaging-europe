---
/**
 * The HA-fillers guide (/fillers) — built on the collagen guide's layout:
 * evidence strength as the primary visual signal, all content in the DOM,
 * one interactive block (concern matcher).
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import MediaPlaceholder from '../components/MediaPlaceholder.astro';
import ExpandableDrawer from '../components/ExpandableDrawer.astro';
import EvidenceRow from '../components/EvidenceRow.astro';
import { DEFAULT_LOCALE } from '../i18n/ui';
import {
  keyTakeaways,
  groups,
  allSections,
  readingMinutes as guideReadingMinutes,
  type Evidence,
} from '../data/fillers';

const locale = DEFAULT_LOCALE;
const title = 'Hyaluronic-acid fillers — the complete guide | DeAging Europe';
const description =
  'Where HA fillers genuinely work, what they cost, how long they really last, and the risks nobody mentions — every area graded by clinical evidence.';

const readingMinutes = guideReadingMinutes();

const evidenceRank: Record<Evidence, number> = { strong: 0, moderate: 1, emerging: 2, limited: 3 };

const evidencePillClass = (e?: Evidence) =>
  e === 'strong'
    ? 'tier-pill tier-strong'
    : e === 'moderate'
    ? 'tier-pill tier-moderate'
    : e === 'emerging'
    ? 'tier-pill tier-emerging'
    : 'tier-pill tier-none';

const orderedSections = (g: (typeof groups)[number]) =>
  g.sections
    .slice()
    .sort((a, b) => evidenceRank[a.evidence ?? 'limited'] - evidenceRank[b.evidence ?? 'limited']);

// Bottom-line answer shown in the collapsed FAQ card.
const faqAnswer: Record<string, string> = {
  'faq-sag': 'No',
  'faq-dissolve': 'Not fully',
  'faq-undo': 'Yes',
  'faq-pregnancy': 'Defer it',
  'faq-vaccine': 'It happens',
  'faq-insurance': 'Emergencies only',
};

const topInfoIds = new Set(['basics', 'context']);
const infoGroupIds = new Set([...topInfoIds, 'safety', 'faq']);
const topInfoGroups = groups.filter((g) => topInfoIds.has(g.id));
const mainGroups = groups.filter((g) => !topInfoIds.has(g.id));
const evidenceGroups = mainGroups.filter((g) => !infoGroupIds.has(g.id));
const tailInfoGroups = mainGroups.filter((g) => infoGroupIds.has(g.id));
const lastChartGroupId = evidenceGroups[evidenceGroups.length - 1].id;
const partNo = (g: (typeof groups)[number]) => mainGroups.findIndex((x) => x.id === g.id) + 1;

// Interactive matcher — concern → recommended rows (anchors must exist in data).
const matcher: { concern: string; paths: { id: string; title: string; why: string }[] }[] = [
  {
    concern: 'Deep smile lines',
    paths: [
      { id: 'use-cheeks', title: 'Cheeks & midface', why: 'restoring the cheek above often softens the fold at the source' },
      { id: 'use-nasolabial', title: 'Nasolabial folds', why: 'direct treatment — the most-trialled filler area of all' },
    ],
  },
  {
    concern: 'Flattened cheeks / tired midface',
    paths: [{ id: 'use-cheeks', title: 'Cheeks & midface', why: 'the strongest evidence and the longest duration (up to 2 years)' }],
  },
  {
    concern: 'Thin or fading lips',
    paths: [{ id: 'use-lips', title: 'Lips & lip lines', why: 'two pivotal RCTs; keep volumes ≤1 ml per session' }],
  },
  {
    concern: 'Under-eye hollows',
    paths: [
      { id: 'use-tear-trough', title: 'Tear troughs', why: 'works in expert hands — read the safety caveats first' },
      { id: 'safety-nodules', title: 'Delayed complications', why: 'this area produces the most late swelling and nodules' },
    ],
  },
  {
    concern: 'Weak chin or soft jawline',
    paths: [
      { id: 'use-chin', title: 'Chin projection', why: 'dedicated pivotal RCT; changes profile harmony most per ml' },
      { id: 'use-jawline', title: 'Jawline definition', why: 'newer approval — works on the line, not on jowls' },
    ],
  },
  {
    concern: 'Crepey, dull, dehydrated skin',
    paths: [
      { id: 'booster-skinboosters', title: 'Skin boosters', why: 'hydration from within — the honest expectation is glow, not lift' },
      { id: 'booster-profhilo', title: 'Profhilo', why: 'read this before paying: its sham-controlled trial failed' },
    ],
  },
  {
    concern: 'Aging hands',
    paths: [{ id: 'use-hands', title: 'Backs of the hands', why: 'randomized split-hand trial; pairs well with pigment lasers' }],
  },
  {
    concern: 'A bump or droop on my nose',
    paths: [{ id: 'use-nose', title: 'Non-surgical rhinoplasty', why: 'high satisfaction — and the highest serious-risk zone. Risk-first reading.' }],
  },
];

// Brand-family comparison cards.
const brandRows = [
  {
    id: 'use-cheeks',
    brand: 'Juvéderm (Allergan)',
    evidence: 'strong' as Evidence,
    blurb: 'Vycross line (Voluma, Volux, Volbella) — most-trialled family; slightly higher swelling rates in the network meta-analysis, slowest to dissolve.',
    knownFor: 'Cheeks, chin, jawline, lips',
  },
  {
    id: 'use-lips',
    brand: 'Restylane (Galderma)',
    evidence: 'strong' as Evidence,
    blurb: 'NASHA + flexible OBT gels (Lyft, Kysse, Defyne) — first HA ever approved; Kysse matched Volbella with ~20% less product; dissolves fastest.',
    knownFor: 'Lips, folds, the only HA approved for hands',
  },
  {
    id: 'use-tear-trough',
    brand: 'Belotero (Merz)',
    evidence: 'strong' as Evidence,
    blurb: 'Cohesive matrix gels that tolerate superficial placement with low Tyndall risk — a frequent choice for delicate, thin-skin areas.',
    knownFor: 'Fine lines, superficial planes',
  },
  {
    id: 'use-perioral',
    brand: 'Teosyal RHA (Teoxane)',
    evidence: 'strong' as Evidence,
    blurb: '"Resilient HA" engineered for movement — two US pivotal trials (434 subjects) in dynamic wrinkles; near-identical results needle vs cannula.',
    knownFor: 'Mobile areas: perioral, marionette',
  },
];
---

<BaseLayout title={title} description={description} locale={locale} path="fillers" type="article">
  <article class="bg-bone">
    <!-- ════════════ HERO ════════════ -->
    <header class="relative">
      <div class="max-w-[1440px] mx-auto px-5 md:px-14 py-14 md:py-20 grid lg:grid-cols-[1fr_auto] gap-10 items-center">
        <div>
          <h1 class="font-display text-5xl md:text-7xl leading-[1.02] max-w-3xl">
            Fillers, without the filler.
          </h1>
          <p class="mt-6 max-w-2xl text-lg md:text-xl text-ink/75 leading-relaxed">
            Where hyaluronic-acid fillers genuinely work, what they cost, how long they really last
            &mdash; and the risks the consult skips.
          </p>
          <p class="meta">
            Updated 3 Sep 2026 · ~{readingMinutes} min full read · {allSections().length} sections
          </p>
        </div>
        <MediaPlaceholder kind="image" ratio="4/5" label="Hero portrait" class="w-full lg:w-72" />
      </div>
    </header>

    <!-- ════════════ TOP-FOLD SUMMARY + INFO DRAWERS ════════════ -->
    <section id="takeaways" class="scroll-mt-32 max-w-[1440px] mx-auto px-5 md:px-14 py-12 space-y-10">
      <div>
        <p class="eyebrow">The case in five lines</p>
        <h2 class="text-3xl mt-2">If you read nothing else</h2>
        <ol class="mt-5 space-y-3 text-[15px] leading-relaxed text-ink/85 max-w-3xl">
          {keyTakeaways.map((k, i) => (
            <li class="flex gap-3">
              <span class="font-display text-accent w-6 shrink-0">{i + 1}.</span>
              <span>{k}</span>
            </li>
          ))}
        </ol>
      </div>

      <div>
        <MediaPlaceholder kind="video" ratio="16/9" label="Explainer · 2 min" />
      </div>

      {topInfoGroups.map((g) => (
        <section id={g.id} class="scroll-mt-32 pt-2">
          <h2 class="text-[32px] md:text-[44px] leading-none">{g.title}</h2>
          <div class="mt-6 grid">
            {g.sections.map((s) => (
              <ExpandableDrawer id={s.id} title={s.title} bodyHtml={s.bodyHtml} />
            ))}
          </div>
        </section>
      ))}
    </section>

    <!-- ════════════ MAIN CONTENT (the graded chart) ════════════ -->
    <section>
      <div class="max-w-[1440px] mx-auto px-5 md:px-14 py-12">

        <div class="mb-10 pt-7 border-t-2 border-ink">
          <p class="eyebrow">The full breakdown</p>
          <h2 class="text-4xl md:text-5xl lg:text-6xl mt-2 leading-[1.05]">HA fillers — what the evidence says</h2>
        </div>

        <div id="guide-body" class="min-w-0">
          {evidenceGroups.map((g) => (
          <>
          <section id={g.id} class="group-section scroll-mt-32 mb-16" data-group={g.id}>
            <p class="eyebrow">Part {String(partNo(g)).padStart(2, '0')}</p>
            <h2 class="text-[32px] md:text-[44px] leading-none mt-2">{g.title}</h2>

            {g.id === 'uses' && (
              <div class="mt-6 grid md:grid-cols-2 gap-4">
                <MediaPlaceholder kind="video" ratio="16/9" label="Inside a midface treatment" />
                <MediaPlaceholder kind="image" ratio="16/9" label="Facial danger-zone map" />
              </div>
            )}

            <div class="chart mt-8">
                {orderedSections(g).map((s) => (
                  <EvidenceRow
                    s={s}
                    figure={g.id === 'uses'}
                  />
                ))}
            </div>
          </section>

          {g.id === lastChartGroupId && (
            <div id="picks" class="band bleed scroll-mt-32 mb-16 py-12 md:py-14">
              <p class="eyebrow">Editor's choice</p>
              <h2 class="text-[32px] md:text-[44px] leading-none mt-2">Our picks</h2>
              <p class="mt-3 text-ink/70 max-w-2xl">Where the evidence, longevity, and safety margins line up best.</p>

              <div class="tiles mt-8 sm:grid-cols-2 lg:grid-cols-4">
                {[
                  {
                    id: 'use-cheeks',
                    kind: 'Best evidence',
                    title: 'Midface volumization',
                    blurb: 'The pivotal-trial champion — visible for up to 2 years and it lifts the lower face with it.',
                  },
                  {
                    id: 'use-lips',
                    kind: 'Best studied',
                    title: 'Conservative lip refresh',
                    blurb: '≤1 ml with a flexible gel — two RCTs behind it and a year of effect.',
                  },
                  {
                    id: 'alt-hyaluronidase',
                    kind: 'The exit',
                    title: 'Hyaluronidase reversal',
                    blurb: 'The reason to choose HA at all: mistakes and regrets are dissolvable.',
                  },
                  {
                    id: 'alt-biostimulators',
                    kind: 'The alternative',
                    title: 'Sculptra biostimulation',
                    blurb: 'For diffuse volume loss when you want collagen, not gel — slower, longer, irreversible.',
                  },
                ].map((pick) => (
                  <a
                    href={`#${pick.id}`}
                    class="pick-card tile tile--flush group overflow-hidden"
                  >
                    <MediaPlaceholder kind="image" ratio="4/3" label={pick.title} />
                    <div class="p-5 flex flex-col flex-1">
                      <p class="eyebrow">{pick.kind}</p>
                      <h3 class="text-xl leading-tight mt-1 transition-colors">{pick.title}</h3>
                      <p class="mt-2 text-sm text-ink/65 leading-snug flex-1">{pick.blurb}</p>
                      <span class="cta mt-4 self-start">
                        View details
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <line x1="5" y1="12" x2="19" y2="12"></line>
                          <polyline points="12 5 19 12 12 19"></polyline>
                        </svg>
                      </span>
                    </div>
                  </a>
                ))}
              </div>
            </div>
          )}
          </>
        ))}
        </div>
      </div>
    </section>

    <!-- ════════════ SAFETY / FAQ TAIL + MATCHER / BRANDS / REFERENCES ════════════ -->
    <div class="max-w-[1440px] mx-auto px-5 md:px-14 py-12">

        {tailInfoGroups.map((g) => (
          <section id={g.id} class="group-section scroll-mt-32 mb-16" data-group={g.id}>
            <p class="eyebrow">Part {String(partNo(g)).padStart(2, '0')}</p>
            <h2 class="text-[32px] md:text-[44px] leading-none mt-2">{g.title}</h2>
            <div class="mt-8 grid">
              {g.sections.map((s) => (
                <ExpandableDrawer id={s.id} title={s.title} short={faqAnswer[s.id] ?? s.tldr} bodyHtml={s.bodyHtml} />
              ))}
            </div>
          </section>
        ))}

        <!-- ════════════ CONCERN MATCHER ════════════ -->
        <section id="matcher" class="scroll-mt-32 mb-16 pt-8 border-t border-ink">
          <p class="eyebrow">Interactive</p>
          <h2 class="text-[32px] md:text-[44px] leading-none mt-2">Match a treatment to your concern</h2>
          <p class="mt-2 text-ink/70 max-w-2xl">Open your main concern to see where the evidence points — each link jumps to the graded section.</p>

          <div class="mt-8 grid">
            {matcher.map((m) => (
              <details class="matcher-item group border-t border-ink last:border-b">
                <summary class="cursor-pointer flex items-center justify-between gap-4 py-3.5 min-h-[44px]">
                  <h3 class="text-[19px] md:text-2xl leading-tight group-hover:text-accent group-open:text-accent transition-colors">{m.concern}</h3>
                  <span class="shrink-0 w-10 h-10 md:w-11 md:h-11 flex items-center justify-center text-accent group-hover:text-ink transition" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="square" stroke-linejoin="miter" class="transition-transform group-open:rotate-180">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </span>
                </summary>
                <div class="pb-5">
                  {m.paths.map((p) => (
                    <a href={`#${p.id}`} class="block p-3 bg-bone/50 border border-ink/5 hover:border-accent/40 transition">
                      <span class="block font-display font-medium text-[17px] tracking-tight leading-snug group-hover/path:text-accent transition-colors">{p.title}</span>
                      <span class="block mt-1 text-sm text-ink/65 leading-normal">{p.why}</span>
                    </a>
                  ))}
                </div>
              </details>
            ))}
          </div>
        </section>

        <!-- ════════════ BRAND FAMILIES ════════════ -->
        <section id="compare" class="scroll-mt-32 mb-16 pt-8 border-t border-ink">
          <p class="eyebrow">The product landscape</p>
          <h2 class="text-[32px] md:text-[44px] leading-none mt-2">Brand families compared</h2>
          <p class="mt-2 text-ink/65 max-w-2xl">All four majors are effective and broadly interchangeable in trials — the differences are texture, longevity, and dissolvability.</p>

          <div class="tiles mt-6 sm:grid-cols-2 lg:grid-cols-4">
            {brandRows.map((r) => (
              <a href={`#${r.id}`} class="tile group">
                <div class="flex items-center justify-between gap-2 mb-2">
                  <span class={`text-[10px] uppercase tracking-widest font-semibold px-2 py-0.5 ${evidencePillClass(r.evidence)}`}>{r.evidence}</span>
                </div>
                <h3 class="text-xl leading-tight transition-colors">{r.brand}</h3>
                <p class="mt-1 text-sm text-ink/65 leading-snug">{r.blurb}</p>
                <p class="mt-3 text-xs font-medium text-ink/85">Known for: {r.knownFor}</p>
              </a>
            ))}
          </div>
        </section>

        <!-- ════════════ REFERENCES ════════════ -->
        <section id="references" class="scroll-mt-32 pt-6 border-t border-ink">
          <h2 class="text-[32px] leading-none">References & further reading</h2>
          <p class="mt-2 text-ink/70 text-sm">
            All claims cite peer-reviewed studies, pivotal-trial reports, or regulatory sources,
            linked inline within each section. Primary sources: PubMed/PMC, FDA pivotal summaries,
            Dermatologic Surgery, Aesthetic Surgery Journal, UK parliamentary briefings.
          </p>
        </section>

        <p class="mt-10 text-xs text-ink/50 italic max-w-3xl">
          Educational content, not medical advice. Filler is a medical procedure — consult a
          qualified clinician, disclose your full medical history, and defer treatment during
          pregnancy or breastfeeding.
        </p>
    </div>

  </article>

  <style>
    details > summary::-webkit-details-marker { display: none; }
    details > summary { list-style: none; }

    details > summary:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: -2px;
    }

    .card[open] > summary { background: rgba(18,18,18,0.03); }

    section[id], details[id] {
      scroll-margin-top: calc(var(--header-h, 60px) + var(--bar-h, 56px) + 12px);
    }

    @media print {
      aside, .pointer-events-none { display: none !important; }
      details { break-inside: avoid; }
      .card { break-inside: avoid; border: 1px solid #ddd !important; box-shadow: none !important; }
    }
  </style>

  <script is:inline>
    (function () {
      const guideBody = document.getElementById('guide-body');
      if (!guideBody) return;

      function measureChrome() {
        const header = document.querySelector('body > header');
        document.documentElement.style.setProperty('--header-h', (header?.getBoundingClientRect().height || 60) + 'px');
        document.documentElement.style.setProperty('--bar-h', '0px');
      }
      measureChrome();
      window.addEventListener('resize', measureChrome);

      function openHashTarget() {
        if (!location.hash) return;
        const el = document.getElementById(location.hash.slice(1));
        if (el?.tagName === 'DETAILS') el.setAttribute('open', '');
      }
      window.addEventListener('hashchange', openHashTarget);
      openHashTarget();
    })();
  </script>
</BaseLayout>
