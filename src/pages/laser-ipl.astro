---
/**
 * The laser & IPL guide (/laser-ipl) — collagen-guide layout: evidence
 * strength as the primary visual signal, one interactive block (matcher).
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import MediaPlaceholder from '../components/MediaPlaceholder.astro';
import ExpandableDrawer from '../components/ExpandableDrawer.astro';
import EvidenceRow from '../components/EvidenceRow.astro';
import { DEFAULT_LOCALE } from '../i18n/ui';
import { guideMeta, formatDate } from '../data/guides';
import {
  keyTakeaways,
  groups,
  allSections,
  readingMinutes as guideReadingMinutes,
  type Evidence,
} from '../data/laser-ipl';

const locale = DEFAULT_LOCALE;
const guide = guideMeta('laser-ipl');
const title = 'Laser & IPL skin treatments — the evidence guide | DeAging Europe';
const description =
  'From gentle IPL to full CO₂ resurfacing — every laser and light treatment for aging skin graded by evidence, matched to skin tone and downtime budget.';

const readingMinutes = guideReadingMinutes();

const evidenceRank: Record<Evidence, number> = { strong: 0, moderate: 1, emerging: 2, limited: 3 };

const evidencePillClass = (e?: Evidence) =>
  e === 'strong'
    ? 'tier-pill tier-strong'
    : e === 'moderate'
    ? 'tier-pill tier-moderate'
    : e === 'emerging'
    ? 'tier-pill tier-emerging'
    : 'tier-pill tier-none';

const orderedSections = (g: (typeof groups)[number]) =>
  g.sections
    .slice()
    .sort((a, b) => evidenceRank[a.evidence ?? 'limited'] - evidenceRank[b.evidence ?? 'limited']);

const faqAnswer: Record<string, string> = {
  'faq-wrinkles': 'Ablative',
  'faq-ipl-antiaging': 'Colour only',
  'faq-dark-skin': '1064 + Fraxel-class',
  'faq-downtime': 'Hours to weeks',
  'faq-melasma': 'No',
  'faq-isotretinoin': 'Mostly no wait',
};

const topInfoIds = new Set(['basics', 'context']);
const infoGroupIds = new Set([...topInfoIds, 'safety', 'faq']);
const topInfoGroups = groups.filter((g) => topInfoIds.has(g.id));
const mainGroups = groups.filter((g) => !topInfoIds.has(g.id));
const evidenceGroups = mainGroups.filter((g) => !infoGroupIds.has(g.id));
const tailInfoGroups = mainGroups.filter((g) => infoGroupIds.has(g.id));
const lastChartGroupId = evidenceGroups[evidenceGroups.length - 1].id;
const partNo = (g: (typeof groups)[number]) => mainGroups.findIndex((x) => x.id === g.id) + 1;

// Interactive matcher — concern → recommended rows.
const matcher: { concern: string; paths: { id: string; title: string; why: string }[] }[] = [
  {
    concern: 'Deep, etched wrinkles',
    paths: [
      { id: 'laser-full-ablative', title: 'Full-field ablative resurfacing', why: 'the only class that removes rather than softens — read the downtime first' },
      { id: 'laser-fractional-ablative', title: 'Fractional ablative', why: 'most of the effect at a fraction of the recovery' },
    ],
  },
  {
    concern: 'Fine lines, rough texture, big pores',
    paths: [
      { id: 'laser-nafl', title: 'Non-ablative fractional (Fraxel-class)', why: '3–6 sessions, days of downtime, well-tested' },
      { id: 'laser-lowenergy', title: 'Clear + Brilliant / Moxi', why: 'the gentle maintenance version' },
    ],
  },
  {
    concern: 'Sun spots & mottled tone',
    paths: [
      { id: 'pigment-lasers', title: 'Q-switched / picosecond lasers', why: '1–2 sessions clear most lentigines' },
      { id: 'ipl-pigment', title: 'IPL photorejuvenation', why: 'the full-face colour reset, 3–6 sessions' },
    ],
  },
  {
    concern: 'Redness, flushing, visible vessels',
    paths: [
      { id: 'vascular-lasers', title: 'Pulsed-dye / KTP lasers', why: '75–85% vessel clearance in ~3 sessions' },
      { id: 'ipl-pigment', title: 'IPL', why: 'comparable for diffuse rosacea redness in meta-analysis' },
    ],
  },
  {
    concern: 'Melasma',
    paths: [
      { id: 'melasma-lasers', title: 'Lasers for melasma', why: 'adjunct only — read the recurrence and toning-risk story first' },
    ],
  },
  {
    concern: 'Deeper skin tone (Fitzpatrick IV–VI)',
    paths: [
      { id: 'darker-skin', title: 'The safety map', why: 'which devices are safe, risky, and off the menu' },
      { id: 'laser-nafl', title: 'Non-ablative fractional', why: 'the preferred class — ~4% transient PIH per session in careful series' },
    ],
  },
  {
    concern: 'Minimal downtime, please',
    paths: [
      { id: 'ipl-pigment', title: 'IPL', why: 'hours of redness, real colour results' },
      { id: 'combos', title: 'Same-session combinations', why: 'colour + texture in one visit without added downtime' },
    ],
  },
];

// Downtime-ladder comparison cards.
const ladderRows = [
  {
    id: 'ipl-pigment',
    step: 'IPL & vascular',
    downtime: 'Hours',
    evidence: 'strong' as Evidence,
    blurb: 'Colour correction — spots, redness, tone. No change to established wrinkles.',
  },
  {
    id: 'laser-nafl',
    step: 'Non-ablative fractional',
    downtime: '1–3 days + flaking',
    evidence: 'moderate' as Evidence,
    blurb: 'Texture and fine lines over a series — the maintenance workhorse.',
  },
  {
    id: 'laser-fractional-ablative',
    step: 'Fractional ablative',
    downtime: '~1 week',
    evidence: 'strong' as Evidence,
    blurb: 'Real wrinkle and scar remodelling in 1–3 sessions.',
  },
  {
    id: 'laser-full-ablative',
    step: 'Full-field ablative',
    downtime: '2 wks + months pink',
    evidence: 'strong' as Evidence,
    blurb: 'The deepest non-surgical result, lasting years — and the biggest risks.',
  },
];
---

<BaseLayout title={title} description={description} locale={locale} path="laser-ipl" type="article">
  <article class="bg-bone">
    <!-- ════════════ HERO ════════════ -->
    <header class="relative">
      <div class="max-w-[1440px] mx-auto px-5 md:px-14 py-14 md:py-20 grid lg:grid-cols-[1fr_auto] gap-10 items-center">
        <div>
          <h1 class="font-display text-5xl md:text-7xl leading-[1.02] max-w-3xl">
            Lasers, in plain light.
          </h1>
          <p class="mt-6 max-w-2xl text-lg md:text-xl text-ink/75 leading-relaxed">
            From gentle IPL to full resurfacing &mdash; matched to your skin tone, your concern, and
            your downtime budget.
          </p>
          <p class="meta">
            Updated <time datetime={guide.updated}>{formatDate(guide.updated)}</time> · ~{readingMinutes} min full read · {allSections().length} sections
          </p>
        </div>
        <MediaPlaceholder kind="image" ratio="4/5" label="Hero — treatment room" class="w-full lg:w-72" />
      </div>
    </header>

    <!-- ════════════ TOP-FOLD SUMMARY + INFO DRAWERS ════════════ -->
    <section id="takeaways" class="scroll-mt-32 max-w-[1440px] mx-auto px-5 md:px-14 py-12 space-y-10">
      <div>
        <p class="eyebrow">The case in five lines</p>
        <h2 class="text-3xl mt-2">If you read nothing else</h2>
        <ol class="mt-5 space-y-3 text-[15px] leading-relaxed text-ink/85 max-w-3xl">
          {keyTakeaways.map((k, i) => (
            <li class="flex gap-3">
              <span class="font-display text-accent w-6 shrink-0">{i + 1}.</span>
              <span>{k}</span>
            </li>
          ))}
        </ol>
      </div>

      <div>
        <MediaPlaceholder kind="video" ratio="16/9" label="Explainer · 2 min" />
      </div>

      {topInfoGroups.map((g) => (
        <section id={g.id} class="scroll-mt-32 pt-2">
          <h2 class="text-[32px] md:text-[44px] leading-none">{g.title}</h2>
          <div class="mt-6 grid">
            {g.sections.map((s) => (
              <ExpandableDrawer id={s.id} title={s.title} bodyHtml={s.bodyHtml} />
            ))}
          </div>
        </section>
      ))}
    </section>

    <!-- ════════════ MAIN CONTENT (the graded chart) ════════════ -->
    <section>
      <div class="max-w-[1440px] mx-auto px-5 md:px-14 py-12">

        <div class="mb-10 pt-7 border-t-2 border-ink">
          <p class="eyebrow">The full breakdown</p>
          <h2 class="text-4xl md:text-5xl lg:text-6xl mt-2 leading-[1.05]">Laser & IPL — what the evidence says</h2>
        </div>

        <div id="guide-body" class="min-w-0">
          {evidenceGroups.map((g) => (
          <>
          <section id={g.id} class="group-section scroll-mt-32 mb-16" data-group={g.id}>
            <p class="eyebrow">Part {String(partNo(g)).padStart(2, '0')}</p>
            <h2 class="text-[32px] md:text-[44px] leading-none mt-2">{g.title}</h2>

            {g.id === 'resurfacing' && (
              <div class="mt-6 grid md:grid-cols-2 gap-4">
                <MediaPlaceholder kind="video" ratio="16/9" label="Inside a fractional session" />
                <MediaPlaceholder kind="image" ratio="16/9" label="The downtime ladder" />
              </div>
            )}

            <div class="chart mt-8">
                {orderedSections(g).map((s) => (
                  <EvidenceRow
                    s={s}
                    figure={g.id === 'resurfacing'}
                  />
                ))}
            </div>
          </section>

          {g.id === lastChartGroupId && (
            <div id="picks" class="band bleed scroll-mt-32 mb-16 py-12 md:py-14">
              <p class="eyebrow">Editor's choice</p>
              <h2 class="text-[32px] md:text-[44px] leading-none mt-2">Our picks</h2>
              <p class="mt-3 text-ink/70 max-w-2xl">The four treatments that earn their place in most plans.</p>

              <div class="tiles mt-8 sm:grid-cols-2 lg:grid-cols-4">
                {[
                  {
                    id: 'laser-fractional-ablative',
                    kind: 'The workhorse',
                    title: 'Fractional ablative CO₂',
                    blurb: 'Real wrinkle and scar remodelling with a week of downtime, not a month.',
                  },
                  {
                    id: 'ipl-pigment',
                    kind: 'The colour fix',
                    title: 'IPL photorejuvenation',
                    blurb: 'Spots, redness and tone in 3–6 easy sessions — the visible-age reset.',
                  },
                  {
                    id: 'laser-nafl',
                    kind: 'The maintainer',
                    title: 'Fraxel-class fractional',
                    blurb: 'Texture and fine lines on a schedule your calendar tolerates.',
                  },
                  {
                    id: 'vascular-lasers',
                    kind: 'For redness',
                    title: 'Pulsed-dye / KTP',
                    blurb: 'The rosacea and vessel specialists — decades of consistent results.',
                  },
                ].map((pick) => (
                  <a
                    href={`#${pick.id}`}
                    class="pick-card tile tile--flush group overflow-hidden"
                  >
                    <MediaPlaceholder kind="image" ratio="4/3" label={pick.title} />
                    <div class="p-5 flex flex-col flex-1">
                      <p class="eyebrow">{pick.kind}</p>
                      <h3 class="text-xl leading-tight mt-1 transition-colors">{pick.title}</h3>
                      <p class="mt-2 text-sm text-ink/65 leading-snug flex-1">{pick.blurb}</p>
                      <span class="cta mt-4 self-start">
                        View details
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <line x1="5" y1="12" x2="19" y2="12"></line>
                          <polyline points="12 5 19 12 12 19"></polyline>
                        </svg>
                      </span>
                    </div>
                  </a>
                ))}
              </div>
            </div>
          )}
          </>
        ))}
        </div>
      </div>
    </section>

    <!-- ════════════ SAFETY / FAQ TAIL + MATCHER / LADDER / REFERENCES ════════════ -->
    <div class="max-w-[1440px] mx-auto px-5 md:px-14 py-12">

        {tailInfoGroups.map((g) => (
          <section id={g.id} class="group-section scroll-mt-32 mb-16" data-group={g.id}>
            <p class="eyebrow">Part {String(partNo(g)).padStart(2, '0')}</p>
            <h2 class="text-[32px] md:text-[44px] leading-none mt-2">{g.title}</h2>
            <div class="mt-8 grid">
              {g.sections.map((s) => (
                <ExpandableDrawer id={s.id} title={s.title} short={faqAnswer[s.id] ?? s.tldr} bodyHtml={s.bodyHtml} />
              ))}
            </div>
          </section>
        ))}

        <!-- ════════════ CONCERN MATCHER ════════════ -->
        <section id="matcher" class="scroll-mt-32 mb-16 pt-8 border-t border-ink">
          <p class="eyebrow">Interactive</p>
          <h2 class="text-[32px] md:text-[44px] leading-none mt-2">Find your light</h2>
          <p class="mt-2 text-ink/70 max-w-2xl">Open your main concern to see where the evidence points — each link jumps to the graded section.</p>

          <div class="mt-8 grid">
            {matcher.map((m) => (
              <details class="matcher-item group border-t border-ink last:border-b">
                <summary class="cursor-pointer flex items-center justify-between gap-4 py-3.5 min-h-[44px]">
                  <h3 class="text-[19px] md:text-2xl leading-tight group-hover:text-accent group-open:text-accent transition-colors">{m.concern}</h3>
                  <span class="shrink-0 w-10 h-10 md:w-11 md:h-11 flex items-center justify-center text-accent group-hover:text-ink transition" aria-hidden="true">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="square" stroke-linejoin="miter" class="transition-transform group-open:rotate-180">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </span>
                </summary>
                <div class="pb-5">
                  {m.paths.map((p) => (
                    <a href={`#${p.id}`} class="block p-3 bg-bone/50 border border-ink/5 hover:border-accent/40 transition">
                      <span class="block font-display font-medium text-[17px] tracking-tight leading-snug group-hover/path:text-accent transition-colors">{p.title}</span>
                      <span class="block mt-1 text-sm text-ink/65 leading-normal">{p.why}</span>
                    </a>
                  ))}
                </div>
              </details>
            ))}
          </div>
        </section>

        <!-- ════════════ DOWNTIME LADDER ════════════ -->
        <section id="compare" class="scroll-mt-32 mb-16 pt-8 border-t border-ink">
          <p class="eyebrow">The trade</p>
          <h2 class="text-[32px] md:text-[44px] leading-none mt-2">The downtime ladder</h2>
          <p class="mt-2 text-ink/65 max-w-2xl">Results scale with recovery — pick your rung deliberately.</p>

          <div class="tiles mt-6 sm:grid-cols-2 lg:grid-cols-4">
            {ladderRows.map((r) => (
              <a href={`#${r.id}`} class="tile group">
                <div class="flex items-center justify-between gap-2 mb-2">
                  <span class={`text-[10px] uppercase tracking-widest font-semibold px-2 py-0.5 ${evidencePillClass(r.evidence)}`}>{r.evidence}</span>
                </div>
                <h3 class="text-xl leading-tight transition-colors">{r.step}</h3>
                <p class="mt-1 text-sm text-ink/65 leading-snug">{r.blurb}</p>
                <p class="mt-3 text-xs font-medium text-ink/85">Downtime: {r.downtime}</p>
              </a>
            ))}
          </div>
        </section>

        <!-- ════════════ REFERENCES ════════════ -->
        <section id="references" class="scroll-mt-32 pt-6 border-t border-ink">
          <h2 class="text-[32px] leading-none">References & further reading</h2>
          <p class="mt-2 text-ink/70 text-sm">
            All claims cite peer-reviewed studies, systematic reviews, or consensus statements,
            linked inline within each section. Primary sources: PubMed/PMC, Lasers in Surgery and
            Medicine, JAMA Dermatology, ASDS consensus documents.
          </p>
        </section>

        <p class="mt-10 text-xs text-ink/50 italic max-w-3xl">
          Educational content, not medical advice. Device selection depends on your skin, history,
          and goals — assess them with a qualified practitioner, and insist on a test spot when
          skin tone or tan is in play.
        </p>
    </div>

  </article>

  <style>
    details > summary::-webkit-details-marker { display: none; }
    details > summary { list-style: none; }

    details > summary:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: -2px;
    }

    .card[open] > summary { background: rgba(18,18,18,0.03); }

    section[id], details[id] {
      scroll-margin-top: calc(var(--header-h, 60px) + var(--bar-h, 56px) + 12px);
    }

    @media print {
      aside, .pointer-events-none { display: none !important; }
      details { break-inside: avoid; }
      .card { break-inside: avoid; border: 1px solid #ddd !important; box-shadow: none !important; }
    }
  </style>

  <script is:inline>
    (function () {
      const guideBody = document.getElementById('guide-body');
      if (!guideBody) return;

      function measureChrome() {
        const header = document.querySelector('body > header');
        document.documentElement.style.setProperty('--header-h', (header?.getBoundingClientRect().height || 60) + 'px');
        document.documentElement.style.setProperty('--bar-h', '0px');
      }
      measureChrome();
      window.addEventListener('resize', measureChrome);

      function openHashTarget() {
        if (!location.hash) return;
        const el = document.getElementById(location.hash.slice(1));
        if (el?.tagName === 'DETAILS') el.setAttribute('open', '');
      }
      window.addEventListener('hashchange', openHashTarget);
      openHashTarget();
    })();
  </script>
</BaseLayout>
